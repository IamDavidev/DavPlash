import { Route, Routes } from 'react-router-dom';

const PublicRoutes = () => {
	return (
		<Routes>
			<Route path='/' element={<p>homee ....</p>} />
		</Routes>
	);
};

export default PublicRoutes;
